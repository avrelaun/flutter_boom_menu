name: flutter_boom_menu
description: Flutter plugin to implement a Boom Menu, with icons, title, sub title, animated FAB Icon and hide on scrolling.
version: 2.0.0-nullsafety.0
author: Mohanraj <mohanraj153@gmail.com>
  Ralph Noel Bruno <brunoralphjr@gmail.com>
  Kuchienkz <wahyupamengas.developer@gmail.com>
homepage: https://github.com/ralphnoel13/flutter_boom_menu

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_staggered_animations: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The 'pluginClass' and Android 'package' identifiers should not ordinarily
  # be modified. They are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  plugin:
    platforms:
      android:
        package: com.example.flutter_boom_menu
        pluginClass: FlutterBoomMenuPlugin
      ios:
        pluginClass: FlutterBoomMenuPlugin

  # To add assets to your plugin package, add an assets section, like this:
  #  assets:
  #    - images/a_dot_burr.jpeg
  #    - images/a_dot_ham.jpeg
  #
  # For details regarding assets in packages, see
  # https://flutter.dev/assets-and-images/#from-packages
  #
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware.

  fonts:
    - family: Futura
      fonts:
        - asset: assets/fonts/futura.ttf

  # For details regarding fonts in packages, see
  # https://flutter.dev/custom-fonts/#from-packages
